# Repository Normalization

The repository is being consolidated around one network registry, one route model, one canonical bridge model, one Warp model, and one Gateway control plane.

Direct commits are applied to `master`.
